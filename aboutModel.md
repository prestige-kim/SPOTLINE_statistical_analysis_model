# 예측 모델 단계별 분석 (spotline_model.py)

## 1단계: (0\~1주) - 수집 및 학습 인프라 준비기

이 시기는 장표에 적힌 대로 **"예측값을 유저에게 노출하지 않고 데이터를 모으는 시기"**입니다. 모델 내부에서는 이 적은 데이터를 가지고 당장의 정확한 예측보다는 **'데이터 파이프라인의 기초 공사(전처리)'**와 **'에러 없는 최소한의 구동'**에 집중합니다.

### 💡 1. 뼈대가 되는 전처리 작업 (`load_and_preprocess_data` 함수)
이 시기에 백엔드가 매일 CSV 파일(`output.csv`)을 던져주면, 파이썬 스크립트는 이를 받아 머신러닝이 이해할 수 있는 형태로 정제(Data Preprocessing)를 시작합니다. 

*   **시간순 정렬 (`sort_values`)**: 
    `df = df.sort_values('captured_at')`
    머신러닝은 순서를 모릅니다. 따라서 과거 데이터가 미래 예측에 섞여 들어가는 '데이터 누수(Data Leakage)'를 막기 위해 무조건 날짜 오름차순으로 엑셀의 행을 정렬하는 기초 작업을 합니다.
*   **요일/주말 피처 생성 (`day_of_week`, `is_weekend`)**:
    단순한 날짜(2026-06-04)를 그대로 쓰지 않고, 0(월)\~6(일)의 숫자와 주말 여부(0, 1)로 변환합니다. 이는 요식업의 특성상 주말 매출이 뛰는 패턴을 모델이 학습할 수 있도록 미리 '정답의 힌트'를 만들어두는 과정입니다.
*   **핵심 변수 창출 (`shift(1)` / 지연 변수)**:
    `df['prev_day_count'] = df['total_count'].shift(1)`
    **가장 중요한 기초 공사입니다.** 전체 데이터의 행을 한 칸씩 밑으로 내려서(shift), '오늘' 행에 '어제 방문객 수'가 위치하도록 조립합니다. 이 작업 때문에 필연적으로 데이터의 첫 번째 날(오픈 첫날)은 '어제' 데이터가 없으므로 `dropna()`로 버려지게 됩니다. 

### 💡 2. 데이터 가뭄 극복을 위한 생존 로직 (`retrain_and_save_model` 내 분기점)
서비스 초기(0\~1주)에는 학습에 쓸 수 있는 데이터(Train Data)가 1\~5개 남짓으로 극도로 부족합니다. 일반적인 머신러닝 모델에 데이터 3\~4개를 던져주고 최적화(GridSearchCV)를 돌리려고 하면 무조건 에러가 발생하고 서버가 다운됩니다.

이를 방지하기 위해 우리 코드에는 매우 훌륭한 **'안전장치(Fallback)'**가 마련되어 있습니다.

```python
    n_samples = len(X_train)
    # 데이터가 5개 미만(약 1주일 이내)일 경우 n_splits는 1이 됨
    n_splits = 5 if n_samples >= 15 else (2 if n_samples >= 5 else 1)

    if n_splits > 1:
        # (데이터가 충분할 때의 로직)
        ...
    else:
        # [0~1주 차에 타는 로직] 
        # 데이터가 너무 적을 경우 교차검증을 생략하고 기본 모델 학습 (Fallback)
        best_model = Ridge(alpha=1.0)
        best_model.fit(X_train_scaled, y_train)
```

이 코드 덕분에 0\~1주 차에는 모델이 에러를 뿜지 않고 가장 기본이 되는 `Ridge(alpha=1.0)` 모델을 가볍게 학습시켜 디스크에 저장만 해둡니다. **즉, 이때는 훌륭한 예측값을 기대하는 것이 아니라 시스템이 매일매일 정상적으로 파이프라인(스케일링 -> 학습 -> 저장)을 통과하는지 워밍업**하는 시기라고 보시면 됩니다.

### 2단계: (1주\~1달) - 본격적인 회귀 예측 및 동적 최적화 가동

데이터가 최소 7일 치 이상(학습용 Train 데이터 5개 이상) 모이기 시작하면, 모델은 비로소 "진짜 머신러닝"의 형태를 갖추고 유저에게 유의미한 예측값과 신뢰구간을 제공하기 시작합니다.

#### 💡 1. 과적합 방지의 핵심, 릿지 회귀 (Ridge Regression)
왜 수많은 알고리즘 중에 하필 '릿지(Ridge)'를 선택했을까요?
1주~1달 차에는 데이터(행)는 적은데 힌트가 되는 피처(기온, 주말 여부, 날씨 원핫인코딩 등 열)는 상대적으로 많습니다. 이런 상황에서 일반 선형 회귀(Linear Regression)를 돌리면, 우연히 비가 온 하루에 손님이 적었다고 해서 "비가 오면 손님이 무조건 0명이다!"라고 극단적으로 치우치는 **'과적합(Overfitting)'**이 쉽게 발생합니다.
릿지 회귀는 L2 규제(Regularization)를 통해 특정 변수 하나가 결과에 미치는 영향력이 너무 커지지 않도록 억제(패널티 부여)합니다. 즉, 데이터가 부족한 시기에 **가장 보수적이고 안전하게 튀지 않는 예측**을 하기 위한 최적의 선택입니다.

#### 💡 2. 데이터량에 맞춘 '동적 교차검증(TimeSeriesSplit)'
서버에 적용된 파이썬 코드의 가장 우수한 디테일 중 하나입니다. 데이터가 막 7일 치 모였는데 5-Fold(데이터를 5조각으로 나누어 검증)를 하려고 하면 조각이 너무 작아져서 검증 자체가 불가능해지고 에러가 납니다.

```python
    # 데이터 크기에 따른 동적 분할
    n_samples = len(X_train)
    n_splits = 5 if n_samples >= 15 else (2 if n_samples >= 5 else 1)
```
위 코드를 통해 학습 데이터가 5개 이상일 때는 2조각(`n_splits=2`)으로 가볍게 최적화(`GridSearchCV`)를 시작하고, 데이터가 약 3주 치 가까이 쌓여 충분해지면(`15`개 이상) 정상적인 5조각(`n_splits=5`) 최적화로 자동 전환됩니다. 즉, **데이터 누적량에 따라 모델 스스로 시스템 다운 없이 안정적으로 검증 수준을 높여가며 진화**하도록 설계되어 있습니다.

#### 💡 3. 신뢰구간(Min/Max Margin) 제공 로직
이 시기부터는 예측값을 제공할 때 단순한 숫자 하나가 아니라 오차 범위를 함께 줍니다.
```python
    mae = mean_absolute_error(y_test, y_pred) # 모델 평가 오차 (MAE)
    # ... (중략) ...
    margin = max(5, int(round(mae))) # 오차 마진 계산
    # 예측값(expected) ± margin 으로 min, max 반환
```
모델은 테스트 데이터에서 자신이 얼마나 틀렸는지를 나타내는 오차(MAE) 점수를 기억해 둡니다. 그리고 미래를 예측할 때 그 오차만큼 위아래(±) 마진(Margin)을 주어 신뢰구간을 형성합니다. 단, 테스트 운이 좋아서 오차가 너무 작게 나왔더라도 최소 5명의 마진(`max(5, ...)`)은 기본으로 깔아두어, 사장님들이 모델을 과신하지 않고 보수적인 재고 관리를 할 수 있도록 안전장치를 마련했습니다.

### 3단계: (1달\~6달) - 자기회귀(AR) 기반 다일 연쇄 예측

데이터가 충분히 안정화된 이 시기부터는 모델이 단순히 '내일' 하루를 예측하는 것을 넘어, **전체적인 주간 트렌드를 그리는 다일(Multi-day) 예측** 모드로 활약하게 됩니다. 현재 백엔드 연동 코드(`main` 함수)의 핵심 기술이 가장 빛을 발하는 구간입니다.

#### 💡 1. 자기회귀(Autoregressive) 꼬리물기 예측 구조
보통 '모레'의 방문객을 예측하려면 힌트가 되는 '내일'의 실제 방문자 수를 알아야 합니다. 하지만 내일 데이터는 아직 현실에 없기 때문에 알 수가 없죠.
이 한계를 극복하기 위해, 모델은 **'방금 자신이 직접 예측해 낸 내일의 결과값을, 다시 모레 예측의 힌트(입력값)로 덮어씌워 재사용'**하는 기법을 사용합니다.

```python
    # 처음에는 오늘 실제 방문자 수를 힌트로 시작
    prev_count = args.today_count 

    for i, day in enumerate(days): # 여러 날짜(예: 7일 치 날씨) 반복
        # [1] _predict_single: 과거 방문객 수(prev_count)를 힌트로 예측 수행
        expected = _predict_single(..., prev_count, ...)
        
        # [2] 핵심 로직: 방금 예측된 값(expected)을 다음 날짜 예측의 힌트(prev_count)로 교체!
        prev_count = expected 
```
이 꼬리물기 로직 덕분에 백엔드 서버가 향후 일주일 치 날씨 배열만 한 번 던져주면, 파이썬 스크립트 내부에서 메모리에 띄워둔 모델 하나만으로 일주일 치 예보를 순식간에 계산해서 한 번에 반환할 수 있게 됩니다. (서버 리소스 절감 및 성능 극대화)

#### 💡 2. 오차 누적(Error Propagation)에 대한 완벽한 방어
하지만 꼬리를 무는 연쇄 예측에는 치명적인 단점이 하나 있습니다. 바로 **'어제 예측이 틀리면, 그 틀린 값을 재사용한 오늘 예측은 더 크게 틀린다'**는 것입니다. 시간이 지날수록 오차가 눈덩이처럼 불어나는(Error Propagation) 현상이 생길 수밖에 없습니다.

바로 이 지점에서 앞선 (1주\~1달) 단계에서 미리 만들어둔 **'MAE 기반 신뢰구간(Min/Max 마진)'**이 매우 강력한 방패가 됩니다.
연쇄 예측으로 인해 후반부 예측값이 조금 흔들리더라도, 모델은 예측값 단일 숫자가 아닌 넉넉한 오차 범위(`margin = max(5, int(round(mae)))`)와 함께 결과를 제공합니다. 따라서 사장님들 입장에서는 오히려 예측이 빗나갈 리스크를 사전에 파악하고, 가장 현실적이고 안전한 예측 범위(예: "이번 주말은 대략 50\~70명 사이겠구나")를 안내받게 됩니다.

#### 💡 3. 완전한 5-Fold 최적화 (안정성 극대화)
이 시기에는 데이터가 한 달 이상(30개 이상) 충분히 쌓였기 때문에, 코드 내부의 `n_splits = 5 if n_samples >= 15 ...` 로직에 따라 항상 **5-Fold TimeSeriesSplit (데이터를 5개의 시계열 조각으로 쪼개서 정교하게 검증)**이 작동합니다. 즉, 외부 환경 변화나 이상치에 쉽게 흔들리지 않는 가장 탄탄한 릿지(Ridge) 모델이 완성된 상태로 구동됩니다.

### 4단계: (6달+) - 딥러닝(LSTM, XGBoost) 전환 로드맵

오픈 후 반년이 지나면 매장의 방문객 데이터는 수천\~수만 건 수준의 방대한 빅데이터로 변모합니다. 이때가 되면 가볍고 방어적이던 릿지(Ridge) 모델을 더 무겁고 정교한 고급 모델로 교체해야 하는 타이밍이 옵니다.

#### 💡 1. 단순 선형 통계 모델(릿지)의 한계 도달
릿지 회귀는 훌륭하지만 기본적으로 '선형(Linear)' 모델입니다. 데이터가 반년 치 이상 쌓이면 "봄비가 내릴 때의 손님 수"와 "겨울비가 내릴 때의 손님 수"가 다르다는 복잡한 계절성이나, "크리스마스이브인데 평일일 때" 같은 비선형적인 패턴이 나타납니다. 릿지 모델은 이런 복잡한 교차 패턴을 모두 담아내기에는 그릇이 작아 성능의 한계(Underfitting)에 부딪히게 됩니다.

#### 💡 2. 왜 LSTM과 XGBoost인가?
*   **LSTM (시계열 특화 딥러닝)**: 시계열 예측에 있어서 끝판왕으로 불리는 딥러닝 아키텍처입니다. '과거의 복잡한 패턴을 얼마나 오래, 잘 기억할 것인가'에 특화되어 있어, 반년 전 데이터의 흐름과 주기성(계절성, 특정 이벤트 패턴)을 끄집어내어 미래를 예측하는 데 압도적인 성능을 발휘합니다.
*   **XGBoost (앙상블 머신러닝)**: 표(테이블) 형태로 된 정형 데이터 분석 생태계의 챔피언입니다. 수많은 의사결정 나무를 겹겹이 쌓아 올려, 날씨와 요일, 기온 간의 매우 복잡하고 미묘한 상호작용(예: "기온이 30도 이상이면서 비가 올 때의 특정 패턴")을 스스로 완벽하게 찾아냅니다. 

데이터가 폭발하는 6개월 시점에는 이 두 모델을 경연시켜 더 성능이 좋은 '챔피언 모델'로 예측의 질을 한 차원 더 끌어올리게 됩니다.

#### 💡 3. 매끄러운 전환을 대비한 코드 아키텍처 (현 시스템의 장점)
가장 중요한 포인트는, 모델을 딥러닝으로 바꾸더라도 **현재 구축된 우리 서비스의 백엔드(Spring Boot)나 전체 파이프라인 구조를 뜯어고칠 필요가 없다**는 점입니다.

현재 작성된 `spotline_model.py` 코드를 보면, 
1. `retrain_and_save_model`에서 모델을 훈련해 `.pkl` 파일로 굽고, 
2. 외부(백엔드)에서는 `_predict_single` 등을 통해 그 파일만 읽어와서 예측만 수행하도록 **"학습과 추론이 완벽하게 분리"**되어 있습니다.

즉, 미래에 6개월이 지났을 때 개발자는 파이썬 코드 내부의 `Ridge()` 부분을 `XGBRegressor()`나 `LSTM()` 객체로 갈아 끼우기만(Plug-in) 하면 끝입니다. 기존 인프라에 충격 없이 언제든 더 나은 두뇌로 업그레이드할 수 있는 유연하고 확장성 높은 구조(Scalable Architecture)를 이미 완성해 두었습니다.
