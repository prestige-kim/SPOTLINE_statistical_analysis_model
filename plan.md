# 📊 SPOTLINE 릿지 회귀 분석 모델 발표 계획 (Presentation Plan)

## 1. 목업 데이터 소개 (`output.csv`)
이번 발표에 사용되는 `output.csv` 목업 데이터는 매장의 약 30일간의 일일 운영 지표 및 환경 데이터를 담고 있는 시계열 데이터입니다.
- **매장 운영 지표**: 
  - `total_count`: 하루 총 방문/주문 건수
  - `avg_dwell_time`: 고객 평균 체류 시간
  - `just_left_count`: 대기 중 이탈한 고객 수
  - `max_empty_table_time`: 빈 테이블 최대 방치 시간
  - `max_response_wait_time`: 최대 고객 응대 대기 시간
  - `peak_time`: 일별 피크 타임 시간대
- **핵심 고객 정보**: `core_customer_age` (주 고객층 연령), `core_customer_gender` (주 고객층 성별)
- **외부 요인**: `temperature` (기온), `weather` (날씨 상황)
- **메타데이터**: `id`, `captured_at`, `created_at`, `end_at` (데이터 수집 및 기록 타임스탬프)

---

## 2. 피처 엔지니어링 (데이터 선별 과정)
모델의 정확도를 높이기 위해 유의미한 변수를 선택하고, 노이즈가 될 수 있는 변수를 제거하는 과정을 거쳤습니다.

### 🟢 선택된 피처 (Selected Features)
- **`temperature`, `weather`**: 날씨와 기온은 고객의 외출 및 매장 방문 수요(`total_count`)에 직접적인 영향을 미치는 중요한 외부 변수입니다.
- **`avg_dwell_time`, `max_empty_table_time`, `max_response_wait_time`**: 매장의 회전율과 직원들의 대응 효율성을 나타냅니다. 이탈 고객 수나 향후 방문 예측에 유의미한 패턴을 제공합니다.
- **`peak_time`**: 시간대별 혼잡도 패턴을 분석하여 일일 총량을 예측하는 데 기여합니다.

### 🔴 제외된 피처 (Dropped Features)
- **`id`, `captured_at`, `created_at`, `end_at`**: 단순 식별 번호 및 시스템 기록용 타임스탬프입니다. (날짜 자체는 요일 등의 파생 변수로 변환할 수 있으나, 이 로우 데이터 자체는 모델이 학습할 숫자적/범주적 가치가 없어 노이즈 방지를 위해 제거합니다.)
- **`core_customer_age`, `core_customer_gender`**: 현재 목업 데이터 내에서 분산이 거의 없는(예: 모두 30대, 특정 성별 등) 상수 값 형태를 띠고 있어, 모델의 예측력 향상에 기여하지 못하므로 필터링합니다.

---

## 3. 타임라인별 데이터 활용 및 모델링 전략
서비스 런칭 후 시간에 따른 데이터 축적량에 맞춰 최적의 전략을 수립했습니다.

### 📅 0일차 ~ 2주차: 데이터 수집 집중
- **이유**: 머신러닝 모델이 주간 계절성(평일/주말 패턴 등)과 같은 최소한의 유의미한 패턴을 학습하기 위해서는 기본 데이터 풀(최소 14일치)이 필요합니다. 이 시기에는 어설픈 예측보다는 신뢰할 수 있는 데이터 파이프라인 구축과 데이터 수집 자체에 집중합니다.

### 📅 2주차 ~ 1개월차: 릿지(Ridge) 회귀 모델 사용 및 신뢰구간 보고
- **이유**: 14일~30일 치의 데이터는 여전히 통계적으로 '작은 데이터(Small Data)'입니다. 따라서 과적합을 방지하는 릿지 회귀 모델을 적용합니다. 
- **신뢰구간 설정 이유**: 데이터가 적어 예측의 불확실성이 높습니다. 단일 예측값(Point Estimate)만 사장님께 제공하면 예측이 조금만 빗나가도 신뢰를 잃을 수 있습니다. 따라서 **"내일은 40~50팀 방문이 예상됩니다"**와 같이 신뢰구간(Confidence Interval)을 설정하여 보고함으로써, 합리적이고 안전한 식자재 발주 및 인력 배치를 돕습니다.

### 📅 1개월차 ~ 6개월/1년: 딥러닝(Deep Learning) 모델로의 전환
- **이유**: 반년 이상 데이터가 축적되면(수백 개의 데이터 포인트), 비로소 월별 트렌드, 공휴일 특수성, 복잡한 비선형 관계가 데이터에 나타납니다. 릿지 회귀는 이러한 복잡한 패턴을 모두 담아내기에는 한계가 있습니다. 풍부한 데이터가 확보된 이 시점부터는 LSTM, Transformer와 같은 딥러닝 모델을 도입하여 예측 성능을 극대화합니다.

---

## 4. 30일치 데이터에서 '릿지 회귀 모델'이 최고의 선택인 이유
일반적으로 릿지 회귀는 딥러닝이나 앙상블 트리 모델(XGBoost, Random Forest 등)에 비해 알고리즘이 단순하여 "성능이 낮다"고 인식될 수 있습니다. 하지만 **현재 확보된 30일치의 소규모 목업 데이터에서는 오히려 가장 강력하고 적합한 모델**입니다.

1. **과적합(Overfitting) 원천 차단**: 30개의 데이터로 딥러닝과 같은 복잡한 모델을 학습시키면, 모델이 데이터의 사소한 '노이즈(우연한 오차)'까지 모두 외워버립니다. 이 경우 학습 데이터에는 100점일지 몰라도, 내일의 실제 데이터를 예측할 때는 성능이 처참하게 무너집니다.
2. **L2 정규화(Regularization)의 안정성**: 릿지 회귀는 페널티 부여(L2 규제)를 통해 특정 피처의 가중치가 비정상적으로 커지는 것을 억제합니다. 데이터 개수가 적어 발생할 수 있는 극단적인 오차에 휘둘리지 않고, 데이터 전반을 관통하는 **가장 안정적이고 일반화된(Generalized) 선형 패턴**을 찾아내기 때문에 현재 상황에서 최고의 예측 모델이 됩니다.
